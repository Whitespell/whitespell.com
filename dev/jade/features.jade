include ./inc/header.jade

section.section.page-title
  .container
    h2 Features
    h3.h4 Overview

    article.column.half.clear-l
      ul.usp
        li
          span.ss-icon.ss-standard check
          | 5 minute setup
        li
          span.ss-icon.ss-standard check
          | No more setting-up back infrastructures
        li
          span.ss-icon.ss-standard check
          | Reduces average loading time by at least 60%
        li
          span.ss-icon.ss-standard check
          | Collaborate, manage and deploy easily using our cloud panel or CLI
    article.column.half.clear-r
      ul.usp
        li
          span.ss-icon.ss-standard check
          | Premium plans that allow you to scale as you grow
        li
          span.ss-icon.ss-standard check
          | Get automatic mail updates about app data
        li
          span.ss-icon.ss-standard check
          | Integrated monetization and customer support options
    .illustration
      span.ss-icon.ss-standard cloud


section.section.feature
  .container
    article.column.half
      h3.h4.alt 5 minute setup
      p.
        Getting started with Whitespell is amazingly easy.
    article.column.half
      section.code-tabs
        article
          input(type="radio", name="results-tab" checked)#js
          label(for="js") JavaScript
          pre.language-javascript
            code.
              //init
              var ws = new Whitespell('YOUR_API_KEY');

              //get todo's
              myOwnWs.sendData({
                  'task': 'SelectTodos',
                  'name': 'test90'
              }, function(data) {
                  console.log(data);
              });
        article
          input.sft(type="radio", name="results-tab")#sft
          label(for="sft") Swift
          pre.language-javascript
            code.
              //swift code
              var ws = new Whitespell('YOUR_API_KEY');

              //get todo's
              myOwnWs.sendData({
                  'task': 'SelectTodos',
                  'name': 'test90'
              }, function(data) {
                  console.log(data);
              });
        article
          input.oc(type="radio", name="results-tab")#obc
          label(for="obc") Objective-C
          pre.language-javascript
            code.
              //objective-c code
              var ws = new Whitespell('YOUR_API_KEY');

              //get todo's
              myOwnWs.sendData({
                  'task': 'SelectTodos',
                  'name': 'test90'
              }, function(data) {
                  console.log(data);
              });

section.section.feature
  .container
    article.column.half
      h3.h4.alt 5 minute setup
      p.
        He unaffected sympathize discovered at no am conviction principles.
        Girl ham very how yet hill four show. Meet lain on he only size.

include ./inc/footer.jade